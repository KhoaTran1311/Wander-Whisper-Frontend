# Wander Whisper Frontend

To run, first ensure to have node and npm installed

Then `cd Wander-Whisper-Frontend`, and run `npm install`.
Build the project with `npm run build`. Start the server with `npm run dev`.